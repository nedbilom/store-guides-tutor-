<h1>Products#index</h1>
<%# debug @products %>

<%= link_to "Добавить новый продукт", new_product_path if authenticated? %>

<ul>
    <% @products.each do |product| %>
        <li>
            <%= link_to product.name, product %>
        </li>
    <% end %>
</ul>